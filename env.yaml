name: openfe-demo
channels:
  - conda-forge
dependencies:
  - python=3.12
  - openfe=1.11
  - notebook 
  - pip
  - pip:
    # cinnabar v0.6.0 will come packaged with openfe v1.12,
    # but we want to show it off in this demo early!
    - git+https://github.com/OpenFreeEnergy/cinnabar@0.6.0
